---
export interface Props {
  variant?: 'primary' | 'secondary' | 'supporting1' | 'supporting2' | 'supporting3' | 'outline' | 'ghost';
  size?: 'sm' | 'md' | 'lg' | 'xl' | 'square';
  as?: 'button' | 'a';
  href?: string;
  type?: 'button' | 'submit' | 'reset';
  class?: string;
  [key: string]: any;
}

const {
  variant = 'primary',
  size = 'md',
  as = 'button',
  href,
  type = 'button',
  class: className = '',
  ...props
} = Astro.props;


const baseStyles = 'button-base'

const variants = {
  primary: 'button-primary',
  secondary: 'button-secondary',
  supporting1: 'button-supporting1',
  supporting2: 'button-supporting2',
  supporting3: 'button-supporting3',
  outline: 'button-outline',
  ghost: 'button-ghost',
};

const sizes = {
  sm: 'button-size-sm',
  md: 'button-size-md',
  lg: 'button-size-lg',
  xl: 'button-size-xl',
  square: 'button-size-square'
};

const classes = `${baseStyles} ${variants[variant as keyof typeof variants]} ${sizes[size as keyof typeof sizes]} ${className}`;

const Tag = as === 'a' ? 'a' : 'button';

---

{
	Tag === 'a' ? (
		<a data-component href={href} class={classes} {...props}>
			<slot />
		</a>
	) : (
		<button type={type} class={classes} {...props}>
			<slot />
		</button>
	)
}
