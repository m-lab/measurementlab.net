---
import FilterableContent from '@components/organisms/Filter/FilterableContent';
import { getPublications, preparePublicationsCardData } from '@utils/publications';

const publications = await getPublications();
const publicationsData = await preparePublicationsCardData(publications);

// // Helper to format category for display
// const formatCategory = (category: string) => {
// 	return category
// 		.split('-')
// 		.map((word) => word.charAt(0).toUpperCase() + word.slice(1))
// 		.join(' ');
// };


---

<div class="pb-12">
	<FilterableContent
		client:load
		type="publications"
		items={publicationsData}
		fields={['category', 'tags', 'year']}
	/>
</div>
