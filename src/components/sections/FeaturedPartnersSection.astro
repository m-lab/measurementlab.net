---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import Button from '@components/atoms/Button.tsx';
import Heading from '@components/atoms/Heading.astro';
import Text from '@components/atoms/Text.astro';
import type { ButtonType } from '@content-config';
import { isVisible } from '@utils/content';

export interface Props {
	title?: string;
	description?: string;
	button?: ButtonType;
}

const { title, description, button } = Astro.props;

// The CTA was hardcoded before it was an editable field; keep those exact values as
// the fallback so homepage entries without a `button` render as they always have.
const cta: ButtonType = button ?? {
	variant: 'primary',
	size: 'md',
	icon: 'arrowRight',
	href: '/partner-with-us',
	text: 'Partner with us',
};

const PARTNER_LIMIT = 9;

// Featured first, then by `order`, then alphabetically by name.
const featuredPartners = (await getCollection('partners'))
	.filter((p) => isVisible(p))
	.sort((a, b) => {
		if (a.data.featured !== b.data.featured) {
			return a.data.featured ? -1 : 1;
		}
		if (a.data.order !== b.data.order) {
			return a.data.order - b.data.order;
		}
		return a.data.name.localeCompare(b.data.name);
	})
	.slice(0, PARTNER_LIMIT);
---

<div class="grid grid-cols-1 items-center gap-12 lg:grid-cols-2">
	<div class="order-2 lg:order-1">
		<div class="grid grid-cols-2 items-center gap-4 sm:grid-cols-3">
			{
				featuredPartners.map((partner) => (
					<a
						href={partner.data.url}
						target="_blank"
						rel="noopener noreferrer"
						class="flex items-center justify-center p-4 transition-all duration-300 hover:scale-105"
					>
						{partner.data.image && (
							<Image
								src={partner.data.image}
								alt={partner.data.name}
								class="max-h-16 w-auto object-contain"
							/>
						)}
					</a>
				))
			}
		</div>
	</div>

	<div class="order-1 flex flex-col gap-8 lg:order-2">
		{
			title && (
				<Heading level={3} class="text-3xl font-bold">
					{title}
				</Heading>
			)
		}
		{description && <Text class="text-gray-600 text-lg">{description}</Text>}

		<div>
			<Button
				as="a"
				href={cta.href}
				variant={cta.variant}
				size={cta.size}
				icon={cta.icon}
			>
				{cta.text}
			</Button>
		</div>
	</div>
</div>
