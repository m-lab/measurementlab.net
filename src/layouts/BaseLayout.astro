---
import '@styles/global.css';
import Head from '@components/organisms/Head.astro';

export interface Props {
  title: string;
  description?: string;
  image?: ImageMetadata;
  lang?: string;
  class?: string;
}

const {
  title,
  description,
  image,
  lang = 'en',
  class: className = '',
} = Astro.props;

const canonicalURL = Astro.site
  ? new URL(Astro.url.pathname, Astro.site)
  : Astro.url;
---

<!doctype html>
<html lang={lang}>
	<head>
		<script is:inline>
			(function() {
				var ua = navigator.userAgent;
				var cls = document.documentElement.classList;
				if (/Mac/.test(ua)) cls.add('os-macos');
				else if (/Win/.test(ua)) cls.add('os-windows');
				else if (/Android/.test(ua)) cls.add('os-android');
				else if (/iPhone|iPad|iPod/.test(ua)) cls.add('os-ios');
				else if (/Linux/.test(ua)) cls.add('os-linux');
			})();
		</script>
		<Head
			title={title}
			description={description}
			canonicalURL={canonicalURL}
			image={image}
		>
			<slot name="head" />
		</Head>
	</head>

	<body class={`bg-neutral-50 ${className}`}>
		<slot />
	</body>
</html>
