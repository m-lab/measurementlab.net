# Astro 7 requires Node >= 22.12.0 (Astro 6 dropped Node 18 and 20).
# Pinned so CI can't silently build on an older default image.
[build.environment]
  NODE_VERSION = "22"

[[headers]]
  for = "/api/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
